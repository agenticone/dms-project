# Environment variables for the DMS project
#
# INSTRUCTIONS:
# 1. Copy this file to a new file named .env
# 2. Fill in your own values in the .env file.
# 3. The .env file is ignored by git and should NOT be committed.

# --- Hostnames for Traefik routing ---
# For local testing, you can use localhost and update your /etc/hosts file.
# For production, use your actual domain names.
TRAEFIK_HOSTNAME=demo.localhost
KEYCLOAK_HOSTNAME=sso.localhost
JBPM_HOSTNAME=workflow.localhost
DMS_HOSTNAME=dms.localhost

# --- Database Passwords ---
# It is highly recommended to change these default passwords.
POSTGRES_PASSWORD=postgrespassword
KEYCLOAK_DB_PASSWORD=keycloakpassword
JBPM_DB_PASSWORD=jbpmpassword
MONGO_PASSWORD=mongopassword

# --- Service Admin Passwords ---
KEYCLOAK_ADMIN_PASSWORD=adminpassword
LDAP_ADMIN_PASSWORD=adminpassword

# --- MongoDB Configuration ---
MONGO_USER=mongo
MONGO_DB=bpdata

# --- Traefik Dashboard Basic Auth ---
# Generate your own with: echo $(htpasswd -nb your_username your_password) | sed -e s/\\$/\\$\\$/g
TRAEFIK_AUTH_USERS=admin:$$apr1$$QVXgZ79u$$Qn/s5tQYyKdX66Vf462K20